project_name: cnpg-cluster

version: 2

# Chart-only repository: goreleaser cuts the GitHub release and the
# changelog; the actual artifacts are the Helm charts, packaged and
# pushed by the shared release workflow (ocictl helmctl) after this.
builds:
  - skip: true

changelog:
  use: github-native
